<% provide(:title, 'Sign up') %>
<!-- 8. 1. 1 少し発展的なパーシャルの使い方 -->
<% provide(:class_text, 'signup') %>
<% provide(:button_text, '新規作成') %>
<h1>ユーザー登録</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
    
    
    <!--全削除 _form.html.erb8. 1 ユーザー編集ページを作ろう -->
    <!--< %= form_with(model: @user, local: true) do |f| %>
    <!--< %= render 'shared/error_messages' %>
 <!-- ↑ 5. 5. 4 登録失敗時のエラーメッセージ-->  
 <!-- ERB（うめこみRuby）は・・・
      <label class="label-signup" for="user_name">Name</label>
      <input class="form-control" type="text" name="user[name]" /> -->
     <!-- <　%= f.label :name, class: "label-signup" %>
      <!--<　%= f.text_field :name, class: "form-control" %>

      < %= f.label :email, class: "label-signup" %>
      < %= f.email_field :email, class: "form-control" %>

      < %= f.label :password, class: "label-signup" %>
      < %= f.password_field :password, class: "form-control" %>

      < %= f.label :password_confirmation, "Confirmation", class: "label-signup" %>
      < %= f.password_field :password_confirmation, class: "form-control" %>

      <　%= f.submit "新規作成", class: "btn btn-primary btn-block btn-signup" %>
    <　% end %>
  </div>
</div> -->